/*
 * Public SADE GitHub App configuration.
 *
 * The Client ID is public. The GitHub App client secret MUST stay in the
 * serverless auth function environment and must never be committed here.
 */
window.SADE_GITHUB_APP = Object.freeze({
  clientId: 'Iv23liwWLsUyHyCgAaD',
  appName: 'SADE AI Engineering Agent',
  authServiceUrl: 'https://sade-github-auth.vercel.app'
});
