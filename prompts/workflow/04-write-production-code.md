# Stage 4 — Write Production Code

Implement the approved plan using production-quality code and the repository's existing conventions.

Reuse existing functionality where appropriate. Maintain backwards compatibility. Handle errors explicitly. Validate external input. Never expose credentials. Do not hard-code secrets or introduce unnecessary dependencies.

For UI work, maintain accessibility, responsiveness and consistent interaction patterns.

After implementation, inspect every modified file. Do not claim completion yet; proceed to testing.